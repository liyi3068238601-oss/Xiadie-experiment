# 遐蝶桌面 Agent 长期开发路线

> 文档状态：助手优先长期执行依据
> 起点版本：v0.1.0
> 目标形态：安全、可控、可扩展的本地优先桌面通用 Agent
> 更新规则：每完成一个小版本，必须同步更新本文件中的状态、验收结果和下一阶段入口。

> 2026-08-01 路线修订：LIFE 生活模拟已退役，不再作为长期候选或可选 Profile。当前先执行 `ASSISTANT_FIRST_ARCHITECTURE_AND_LIFE_RETIREMENT_PLAN.md` 的 RETIRE.0～RETIRE.4，再推进 Task、ToolRegistry、Web/Research、文件与办公工具。本文较早的 LIFE/LIFE v2 内容只保留历史记录。

## 0. 这份文档解决什么问题

本文件把遐蝶从当前可运行的桌面聊天伴侣，逐步推进到完整桌面 Agent 的过程固定下来。

后续开发必须遵循以下节奏：

1. 每次只完成一个边界清楚、可以独立验收的主题。
2. 先让底层安全、可靠、可测试，再增加工具和自动化能力。
3. 先做单主控 Agent Core，再逐步拆出 Worker Agent。
4. 先做只读能力，再做可写能力；先做本地能力，再做外部影响能力。
5. 高风险操作始终需要明确授权、审计记录、取消机制和回滚方案。
6. 不为了追赶路线图而跳过测试、权限、日志、迁移和失败恢复。

设计稿版本号与应用版本号是两套概念。旧设计文档中的 v0.3、v0.5、v0.6 是文档修订号；本路线中的 v0.1.1、v0.2.0、v1.0.0 是实际应用版本。

---

## 1. 产品定位与最终完成定义

### 1.1 一句话定位

遐蝶是一个常驻 Windows 桌面的 AI 伴侣 Agent：她能理解用户意图、记住重要信息、规划任务、调用受控工具、处理本地资料、生成可追溯产物，并在有风险的动作前请求确认。

“伴侣”描述人格和交互体验，不表示产品需要模拟角色离线生活。连续性来自真实对话、记忆、任务、日期、项目和工具结果。

### 1.2 最终用户体验

```text
启动遐蝶
  ↓
桌面只显示固定角色的 Live2D 桌宠
  ↓
用户点击桌宠或使用托盘/快捷键
  ↓
打开单一遐蝶主窗口
  ↓
用户可以聊天、交付资料、创建任务或要求执行工作
  ↓
Agent 分析目标并形成可见计划
  ↓
按权限调用文件、知识、浏览器、办公和外部连接工具
  ↓
高风险步骤弹出确认卡，用户可以允许、拒绝、修改或停止
  ↓
执行过程持续显示进度、日志、下一动作和输出产物
  ↓
Verifier 检查结果，失败时安全重试或请求用户介入
  ↓
结果、来源、产物、记忆和审计记录均可查看、导出和删除
```

### 1.3 最终 Agent 必须具备的能力

- 陪伴式多轮聊天和稳定角色表达。
- 多模型 Provider 配置、能力识别和可解释路由。
- 可控的短期上下文、长期记忆和用户画像。
- 本地文件读取、编辑、生成和产物管理。
- PDF、Word、Excel、Markdown、文本和图片资料处理。
- 可追溯知识库、语义检索和回答引用。
- 任务规划、步骤执行、暂停、继续、取消、重试和重新规划。
- 搜索、浏览器、办公、网络 API 和外部消息等工具能力。
- 分级权限、逐次确认、最小授权、急停和审计。
- 单主控与多个专业 Worker 的渐进式协作。
- 故障诊断、结果验证、幂等处理和安全恢复。
- 数据导出、备份、恢复、迁移和隐私删除。
- Windows 安装、升级、日志收集和稳定发布。

### 1.4 最终 Agent 明确不能做的事

- 不得绕过用户确认执行高风险动作。
- 不得静默发送消息、付款、下单、删除数据或修改生产系统。
- 不得在用户不知情时扫描硬盘、上传文件或保存敏感记忆。
- 不得把网页、文件或外部工具返回的内容当作更高优先级系统指令。
- 不得自行扩大目录、账号、应用或网络权限。
- 不得静默修改自身代码、自动部署或自动覆盖稳定版本。
- 不得使用授权不清楚的 Live2D、角色、美术、音频或模型资源对外发布。
- 不得为了“全能”而一次性开放 Shell、输入控制和任意网络访问。

---

## 2. 权威技术与产品决策

### 2.1 冻结的技术路线

| 层级 | 当前及长期选择 | 原则 |
| --- | --- | --- |
| 桌面壳 | Electron | 保留现有实现，不迁移 Tauri。 |
| 前端 | React + TypeScript + Vite | 逐步拆分，不推倒重写。 |
| 后端 | Python + FastAPI | 继续作为 Agent Core 和工具执行底座。 |
| 本地存储 | SQLite | 先保持单机可靠，迁移必须可回滚。 |
| 模型协议 | OpenAI-compatible 为基础 | 通过适配层支持差异，不把供应商逻辑散落到业务层。 |
| 桌宠 | 固定内置 Live2D | 不提供用户导入、替换、市场或路径选择。 |
| UI 结构 | 桌宠入口 + 单主窗口 + 按需面板 | 不走默认多窗口路线。 |
| Agent 形态 | 单主控优先，Worker 渐进拆分 | 先验证工具协议，再引入真实多 Agent。 |

### 2.2 文档参考优先级

如旧文档之间存在冲突，按以下顺序处理：

1. 本文件及仓库内后续 ADR。
2. `遐蝶Agent_v0.2UI回归与Cyrene风格融合迁移方案_v0.6.1`。
3. `遐蝶桌面Agent_UI设计规格书_Cyrene融合回归版_v0.3.1`。
4. `遐蝶桌面Agent_Cyrene路线版总体设计与开发计划_v0.5.1`。
5. `遐蝶Agent_Codex_PR拆分执行计划_v0.6.2`。
6. 旧版 UI、多 Agent、长期路线文档只作细节和远期架构参考。

以下旧路线已明确失效：Tauri、多窗口优先、用户导入 Live2D、早期 QQ、早期桌面自动化、立即拆分多 Agent。

---

## 3. 当前 v0.1.0 基线

### 3.1 已经实现

- Electron 桌宠、托盘、右键菜单、主窗口和状态联动。
- 固定路径 Live2D 加载与模型缺失占位。
- React 三栏主窗口和紫蓝玻璃拟态基础视觉。
- 会话创建、删除、自动标题和消息历史。
- SSE 流式聊天、复制、收藏、重试和重新生成。
- 多个 OpenAI-compatible Provider 的配置、测试和切换。
- Mock 演示模型。
- L0/L1/L2 记忆的增删改查、启用和基础注入。
- 任务创建、状态流转、删除和从对话创建任务。
- 文件与知识、工具记录、权限和数据设置的页面骨架。
- SQLite 本地存储。
- Windows 构建脚本和双击开发启动脚本。
- 后端 11 项测试、前端生产构建和 Electron 脚本检查。

### 3.2 已实现但仍不完整

- Provider 能力标签目前主要通过模型名称猜测，不是可靠能力模型。
- 自动记忆目前是关键词启发式，不具备敏感信息识别和确认流程。
- “今日任务”只是取未完成任务，不是真正的日期调度。
- 工具记录只有表结构和读取页面，没有真实 ToolRegistry 和审计写入。
- 权限页面只有说明，没有后端策略执行和确认闭环。
- Live2D 设置多数是占位，没有真实持久化和 IPC 行为。
- 当前本地 Live2D 素材仅允许个人自用，不能进入公开仓库或正式发行包。

### 3.3 当前关键风险

- 本地 FastAPI 允许任意来源访问，缺少桌面端访问令牌。
- API Key 仍明文存放在 SQLite。
- 重新生成会先删除旧回复，新请求失败时可能丢失内容。
- 聊天历史没有上下文预算和摘要机制。
- 模型选择缺少严格校验。
- 前端缺少系统性的组件测试和 Electron 端到端测试。
- 应用版本、数据库迁移和发布流程尚未形成正式规范。

---

## 4. 每个小任务的统一执行规范

### 4.1 任务大小

- 每次只做一个主题。
- 通常修改不超过 8 至 12 个文件。
- 超过 15 个文件时必须先停止，说明为什么不能继续拆分。
- 不在同一个任务中混合大重构、新功能和视觉改版。
- 不做未在任务范围内的“顺手优化”。

### 4.2 每次开始前

1. 阅读本文件对应阶段。
2. 检查 `git status`，保护用户已有修改。
3. 写清本轮目标、不做事项、风险和验证命令。
4. 确认是否涉及数据库迁移、权限扩大、外部写入或发布行为。
5. 如果行为会改变用户数据，先定义回滚和备份方式。

### 4.3 每次完成后

- 后端修改：运行全部后端测试。
- 前端修改：运行 TypeScript 检查和生产构建。
- Electron 修改：运行语法检查，并按风险进行桌面启动验证。
- 数据库修改：测试新库初始化、旧库迁移、迁移重复执行和失败回滚。
- 权限修改：测试允许、拒绝、超时、取消和越权五种情况。
- 文件修改：测试路径逃逸、大小限制、编码、并发修改和失败恢复。
- 输出修改文件、行为变化、验证结果、已知风险和下一步。
- 形成单一目的提交并推送；阶段完成后再打版本标签。

### 4.4 通用完成标准

一个任务只有同时满足以下条件才算完成：

- 正常路径可用。
- 失败路径有清晰提示。
- 用户取消不会留下半完成状态。
- 日志不泄露密钥、文件正文或敏感参数。
- 无静默权限扩大。
- 测试和构建通过。
- 文档与真实行为一致。

---

## 5. 版本路线总览

| 应用版本 | 核心目标 | 结果形态 |
| --- | --- | --- |
| v0.1.1 | 治理与本地安全 | 可放心继续扩展的可靠基线。 |
| v0.1.2 | 聊天、上下文和数据可靠性 | 长对话和错误恢复稳定。 |
| v0.1.3 | 前端结构与 UI 验收 | 当前伴侣 UI 形成可维护基线。 |
| v0.2.0 | ToolRegistry 与权限内核 | Agent 首次拥有统一工具协议。 |
| v0.2.1 | 本地安全工具闭环 | 任务、记忆、会话可通过工具执行并审计。 |
| v0.3.0 | 本地文件工作区 | 可安全浏览、读取和编辑文本文件。 |
| v0.3.1 | 文档解析与产物管理 | 可处理常见文档并管理输出产物。 |
| v0.4.0 | 可追溯知识库 | 可导入、检索、引用和删除知识。 |
| v0.4.1 | 可靠记忆系统 | 记忆可确认、去重、冲突处理和遗忘。 |
| v0.4.2 | 记忆星座与伴侣状态 | 记忆形成可追溯星图，遐蝶具有可解释且受控的连续状态。 |
| v0.5.0 | TaskRun 执行工作台 | 复杂任务有计划、步骤、进度和急停。 |
| v0.5.1 | Planner / Executor / Verifier | 单主控 Agent 可完成可靠多步骤工作。 |
| v0.6.0 | 搜索与浏览器工具 | 可在授权下查询网页和操作浏览器。 |
| v0.6.1 | 办公与数据工具 | 可生成和处理表格、文档、PDF 等产物。 |
| v0.7.0 | 外部连接与消息 | 可在逐次确认下连接外部系统。 |
| v0.8.0 | 受控桌面自动化 | 可观察并操作白名单应用，具备急停。 |
| v0.9.0 | 多 Agent Worker 化 | 专业 Worker 在统一调度和权限下协作。 |
| v0.9.1 | 恢复、评估和长期运行 | 失败可恢复、质量可度量、运行可诊断。 |
| v1.0.0 | 可发布稳定版 | 安全、可靠、可安装、可迁移的完整桌面 Agent。 |
| v1.x | 多模态、语音和插件生态 | 在稳定内核上按需扩展。 |
| v2.0.0 | 成熟个人 Agent 平台 | 可组合专业能力但始终由用户掌控。 |

---

## 6. v0.1.1：治理与本地安全

目标：不新增“酷功能”，先解决继续扩展会放大的安全问题。

### 6.1 文档治理

1. 创建 `docs/CODEX_PROJECT_CONTEXT.md`，记录不可变决策和禁止事项。
2. 创建 `docs/BASELINE_STATUS.md`，记录当前启动、测试、构建和功能状态。
3. 创建 `docs/PR_CHECKLIST.md`，固定每轮范围和验收模板。
4. 创建 `docs/adr/`，为重要技术决策保留 ADR。
5. 在 README 中链接本路线和当前状态文档。

### 6.2 本地 API 访问保护

1. Electron 每次启动生成高强度随机本地令牌。
2. 令牌通过受控 preload 桥接交给主窗口，不进入 URL、日志和 localStorage。
3. 前端所有 API 请求携带令牌。
4. 后端中间件校验令牌，健康检查只暴露最少信息。
5. CORS 从 `*` 改为明确允许的本地开发源和 Electron 来源策略。
6. 测试无令牌、错误令牌、正确令牌和开发模式。
7. 打包模式下验证令牌传递和后端重启。

### 6.3 Provider 与密钥边界

1. 抽象 SecretStore 接口，不让业务代码直接依赖 SQLite 密钥字段。
2. 开发模式可保留受限兼容层，正式模式使用 Electron `safeStorage`。
3. 明确新增、更新、清除和检测密钥的行为。
4. API 永不返回完整密钥，只返回 `has_key` 和必要状态。
5. 日志、异常和连接测试结果统一脱敏。
6. 设计旧 SQLite 密钥迁移流程：读取、写入安全存储、验证、清除旧值。

### 6.4 本版本验收

- 任意普通网页不能读取或修改本机遐蝶数据。
- Electron 主窗口仍可正常使用所有现有接口。
- API Key 不出现在响应、日志和错误信息中。
- 旧用户数据可继续使用。
- 后端测试、前端构建和桌面启动全部通过。

---

## 7. v0.1.2：聊天、上下文和数据可靠性

### 7.1 重新生成安全化

1. 生成新回复前不删除旧回复。
2. 新回复完整落库后，再以事务替换旧回复或建立版本关系。
3. 生成失败、取消或断网时保留旧内容。
4. 前端允许查看回复版本并恢复上一版。

### 7.2 上下文预算

1. 为每个 Provider/Model 定义上下文能力。
2. 计算系统提示、记忆、历史和当前输入的预算。
3. 优先保留系统规则、最近对话和明确相关记忆。
4. 长会话生成可查看的阶段摘要。
5. 摘要不得覆盖原始消息，只作为派生数据。
6. 模型超限前主动裁剪并向用户提供可诊断信息。

### 7.3 输入与数据校验

1. 限制消息、标题、任务和记忆的长度。
2. 严格校验 Provider 是否存在、是否启用、模型是否属于可用列表。
3. 删除不存在资源时返回明确状态，不伪装成功。
4. 统一 API 错误结构和前端错误卡。
5. 为 SQLite 增加必要索引、事务边界和备份测试。

### 7.4 本版本验收

- 长会话不会无限发送全部历史。
- 重新生成失败不会丢失旧回复。
- 非法模型、空值、超长输入和损坏设置都有明确结果。
- 数据库异常不会让聊天永久失效。

---

## 8. v0.1.3：前端结构与 UI 基线

目标：只整理已经存在的 UI，不重新设计产品。

### 8.1 组件结构

1. `App.tsx` 只保留应用入口和顶层组合。
2. 提取主窗口壳、顶部栏、左侧栏和会话列表。
3. 保留现有 `ChatView`、`RightBar`、任务、记忆、文件和设置页面。
4. API 客户端按领域拆分，但不改变接口行为。
5. 不为了拆分引入大型状态管理库。

### 8.2 样式结构

1. 提取颜色、间距、圆角、阴影、字体和动效 token。
2. 把应用壳、聊天、页面和桌宠样式分区。
3. 保持现有紫蓝玻璃拟态方向。
4. 检查 1280×720、1366×768、1920×1080 和系统缩放。

### 8.3 UI 质量

1. 增加错误边界和页面加载状态。
2. 删除按钮和危险动作统一确认体验。
3. 补键盘焦点、可访问名称和基本快捷键。
4. 建立关键组件测试和最小 Electron E2E 冒烟。
5. 保存基线截图用于后续视觉回归。

### 8.4 本版本验收

- 功能和 API 行为不变。
- 主窗口没有遮挡、溢出和不可滚动区域。
- 核心页面具备空、加载、错误和禁用状态。
- 前端构建、组件测试和桌面冒烟通过。

---

## 9. v0.2.0：ToolRegistry 与权限内核

这是从“聊天应用”变成“Agent”的第一个关键版本。

### 9.1 工具定义

建立统一对象：

```text
ToolDefinition
├─ name / version / description
├─ input_schema / output_schema
├─ risk_level
├─ required_capabilities
├─ timeout / retry_policy
├─ idempotent
├─ preview()
└─ execute()
```

### 9.2 注册表

1. 工具显式注册，不允许按字符串动态导入任意代码。
2. 启动时检查工具名、版本、Schema 和权限声明。
3. 提供列出工具、查询工具和执行工具的内部接口。
4. 第一阶段不接模型 function calling，只用测试和本地命令验证。

### 9.3 权限内核

建议风险等级：

| 等级 | 示例 | 默认策略 |
| --- | --- | --- |
| S0 | 展示、计算、格式转换 | 自动允许并记录。 |
| S1 | 读取用户明确选择的本地范围 | 在已授权范围内允许。 |
| S2 | 修改本地数据或文件 | 每次预览并确认。 |
| S3 | 外发消息、网络写入、操作应用 | 强确认，展示目标和参数。 |
| S4 | Shell、输入控制、系统设置、付款 | 默认禁用，后期白名单开放。 |

权限决策至少包含：工具、风险、资源范围、会话、有效期、参数摘要和用户决定。

### 9.4 审计事件

每次工具调用记录：

- 调用 ID、任务 ID、会话 ID。
- 工具名和版本。
- 风险等级和权限决策。
- 脱敏参数摘要。
- 开始、结束、耗时和状态。
- 结果摘要、错误码和产物引用。
- 用户允许、拒绝、取消或超时。

### 9.5 本版本验收

- 注册一个测试工具即可被发现、预览、执行和审计。
- 未注册工具无法执行。
- S2 以上工具没有批准时无法进入执行函数。
- 拒绝、超时和取消不会留下副作用。

---

## 10. v0.2.1：本地安全工具闭环

### 10.1 第一批工具

- `task.create/list/start/complete/reopen/archive/delete`
- `memory.create/list/update/enable/disable/delete`
- `session.rename/archive`
- `settings.read_safe`
- `system.health`

### 10.2 聊天接入顺序

1. 先保留前端按钮和现有 API 行为。
2. 后端 API 内部改为调用 ToolRegistry。
3. 再增加明确格式的本地聊天命令。
4. 最后才对支持工具调用的模型开放 function calling。
5. 模型提出工具调用时必须重新经过权限层，模型不能自批权限。

### 10.3 前端工具卡

- 显示工具名、目的、参数摘要和风险等级。
- 执行中显示状态和取消入口。
- 完成后显示结果摘要和审计详情入口。
- 需要确认时提供允许一次、拒绝、修改参数；暂不提供“一键永久全开”。

### 10.4 本版本验收

- 从 UI、API 和聊天触发同一工具时行为一致。
- 工具记录页面显示真实调用，不再是空壳。
- 任务、记忆和会话原有功能没有回归。

---

## 11. v0.3.0：安全的本地文件工作区

### 11.1 范围授权

1. 用户通过 Electron 原生目录选择器明确选择工作目录。
2. 授权记录保存规范化绝对路径、时间和作用范围。
3. 所有子路径 resolve 后必须仍处于授权根目录。
4. 不默认扫描其他磁盘、用户目录、浏览器配置或系统目录。

### 11.2 只读阶段

1. 列出目录和文件基础元数据。
2. 进入子目录和返回上级。
3. 读取允许扩展名的文本文件。
4. 限制文件大小、读取时间和编码。
5. 二进制文件只显示元数据，不直接作为文本解析。
6. 路径逃逸、符号链接逃逸和设备路径必须拒绝。

### 11.3 写入阶段

1. 编辑已有文本文件。
2. 创建新文本文件和目录。
3. 保存前展示路径、变更摘要和风险。
4. 支持 `expected_mtime` 或内容哈希，避免覆盖外部修改。
5. 优先原子写入：临时文件、落盘、替换。
6. 删除先进入应用回收区，不直接永久删除。
7. 所有写入、覆盖、移动和删除进入审计日志。

### 11.4 本版本验收

- 不能通过 `..`、绝对路径、符号链接或特殊路径越过工作区。
- 取消确认不会写入。
- 外部修改后保存会提示冲突。
- 大文件、二进制、无权限和编码错误不会导致后端崩溃。

---

## 12. v0.3.1：文档解析与产物管理

### 12.1 文档读取

按顺序支持：

1. TXT / Markdown / JSON / CSV。
2. PDF 文本和页码。
3. DOCX 段落、标题和表格。
4. XLSX 工作表、单元格范围和公式结果。
5. 图片 OCR 作为可选能力，明确准确率限制。

### 12.2 产物对象

产物必须独立于聊天消息：

```text
Artifact
├─ id / task_id / version
├─ type / mime_type
├─ source_refs
├─ local_path
├─ checksum / size
├─ created_by_tool
├─ created_at
└─ status
```

### 12.3 输出规则

- 输出默认进入用户选定目录或应用产物区。
- 覆盖现有文件必须确认。
- 产物支持预览、打开所在目录、另存为和删除。
- 每次重新生成形成版本，不静默覆盖上一版。

### 12.4 本版本验收

- 常见文档可以安全读取并保留来源信息。
- 生成的产物可追踪到任务、工具和输入来源。
- 文档解析失败不会污染知识库或产生伪成功产物。

---

## 13. v0.4.0：可追溯知识库

### 13.1 导入流程

1. 用户明确选择文件或网页。
2. 显示文件类型、大小、数据流向和是否可能调用远程模型。
3. 解析原文并保存文档元数据。
4. 按标题、页、段落和语义边界切片。
5. 生成 Embedding 并写入索引。
6. 索引失败时保留可诊断状态，不伪装完成。

### 13.2 检索流程

1. 查询改写。
2. 关键词与向量混合检索。
3. 权限和文档范围过滤。
4. 去重与重排。
5. 把有限片段注入模型。
6. 回答显示文件、页码或段落来源。

### 13.3 知识库 UI

- 集合、标签、来源类型和索引状态。
- 文档列表和搜索。
- 文档摘要、片段、元数据和版本。
- 引用次数和相关任务。
- 重建索引、移动集合和删除。

### 13.4 删除与隐私

- 删除文档必须同步清除原文缓存、切片、向量和派生摘要。
- 本地 Embedding 和远程 Embedding 必须明确区分。
- 敏感文档默认不发送远程服务。
- 索引库支持导出、备份和完整清除。

### 13.5 本版本验收

- 回答引用可定位到真实来源。
- 找不到证据时明确说明，而不是生成虚构引用。
- 删除后检索不到残留片段。
- 多文档同名、更新和重索引行为明确。

---

## 14. v0.4.1：可靠记忆系统

### 14.1 三层记忆职责

| 层级 | 内容 | 写入策略 |
| --- | --- | --- |
| L0 | 称呼、稳定身份、长期核心偏好 | 必须确认，极少自动修改。 |
| L1 | 当前项目、近期目标、正在进行的状态 | 可建议写入，用户可快速撤销。 |
| L2 | 长期经历、约定、历史摘要 | 经筛选和去重后写入。 |

### 14.2 记忆候选流程

1. 模型只能提出记忆候选，不直接决定永久保存。
2. 候选包含内容、层级、来源、置信度、敏感性和有效期。
3. 敏感、身份、健康、财务、账号信息默认不自动保存。
4. 与已有记忆重复或冲突时进入待确认队列。
5. 用户可以接受、编辑、拒绝、禁用和永久删除。

### 14.3 召回流程

- 按当前任务相关性、层级、时间和启用状态选择。
- 注入数量和 token 有硬上限。
- UI 提示本轮参考了哪些记忆。
- 用户可以从回答跳转到记忆来源并撤销。

### 14.4 本版本验收

- 敏感信息不会因关键词被静默永久保存。
- 重复和冲突记忆不会无限增长。
- 删除或禁用的记忆不再进入上下文。
- 记忆来源和修改历史可追踪。

---

## 14A. v0.4.2：记忆星座与交互边界

本阶段融合 [jiwen](https://github.com/ClaraShafiq/jiwen) 的连续状态思路与 [MemoryConstellations](https://github.com/ClaraShafiq/MemoryConstellations) 的分层记忆组织。详细边界与交付顺序见 `docs/JIWEN_MEMORY_CONSTELLATIONS_INTEGRATION.md` 和 ADR-0002。

### 14A.1 当轮表达与关系边界

- 只在当前请求内理解用户状态并生成有界表达指导。
- Relationship 只保存由真实互动支持的称呼、距离、信任和边界。
- 不跨轮模拟遐蝶心境，不进行离线情绪漂移或联系需求积温。
- 表达指导不能影响事实判断、工具权限和安全策略。

### 14A.2 记忆星座

- Fragment 保留到原消息的来源链。
- Entity 聚合同一人物、项目或概念。
- Episode 表达一次连续经历，Saga 表达跨时间长期主题。
- 星图是数据的可视化入口，不替代列表、搜索、纠正和删除能力。
- 归档和合并必须可审计、可撤销，并受模型调用预算限制。

### 14A.3 Persona、WorldBook 与 ShortMemo 新归属

本节原为 LIFE v2 候选登记，已被 2026-08-01 助手优先路线取代：

- Persona 编译器、模型认证、Chat/Work 模式和 WorldBook 保留，归 Persona/Lore。
- `short-memo-v1` 保留限长、来源、TTL、敏感信息硬门和用户控制，归 Task/CTX/MEM。
- `inner-state-projection-v1`、Goal/LIFE Event 表达投影和 LIFE v2 名义全部退役。
- 详细迁移见 `ASSISTANT_FIRST_ARCHITECTURE_AND_LIFE_RETIREMENT_PLAN.md`。

### 14A.4 本版本验收

- 当前轮表达指导不跨请求持久化。
- 自动记忆均有来源，敏感候选不会静默永久保存。
- 星图节点能定位到真实内容，删除后不会留下可召回的派生数据。
- 主动帮助只由任务、提醒、承诺、重要日期、工具结果或 OpenThread 触发，并具备安静时段、次数上限和一键暂停。

---

## 15. v0.5.0：TaskRun 执行工作台

### 15.1 核心对象

- `Task`：用户目标和长期状态。
- `TaskRun`：一次具体执行。
- `TaskNode`：计划中的一个步骤。
- `ToolCall`：某一步调用工具的记录。
- `Artifact`：执行产生的文件或结构化结果。
- `Approval`：用户针对高风险动作的决定。

### 15.2 状态机

```text
draft
  → planning
  → awaiting_approval
  → running
  → paused
  → completed
  → failed
  → cancelled
```

每个状态必须定义允许进入的下一状态，禁止前端随意写状态字符串。

### 15.3 工作台 UI

- 任务标题、目标、来源和创建时间。
- 执行路径和总体进度。
- Agent 计划、当前步骤和下一动作。
- 工具调用卡和权限确认卡。
- 输出产物预览和版本。
- 操作日志、错误原因和恢复建议。
- 暂停、继续、取消、重新规划和全局急停。

### 15.4 本版本验收

- 应用重启后可以恢复未完成任务的状态。
- 暂停后不会继续调用新工具。
- 取消后正在运行的可取消操作会停止。
- 同一步骤重复执行不会产生不可控重复副作用。

---

## 16. v0.5.1：Planner / Executor / Verifier

### 16.1 Planner

- 把用户目标转换为有限、有依赖关系的步骤。
- 每一步声明输入、输出、工具、风险和完成条件。
- 缺少关键参数时先询问，不凭空决定。
- 计划长度、重规划次数和成本有上限。

### 16.2 Executor

- 只执行已经批准且依赖满足的步骤。
- 每次只获取最小必要能力令牌。
- 执行前再次检查资源范围和参数。
- 记录开始、心跳、结果、产物和错误。

### 16.3 Verifier

- 根据步骤完成条件检查结果，不只相信模型自述。
- 文件结果检查存在性、格式、大小和必要内容。
- 结构化结果检查 Schema。
- 外部动作检查可确认的回执。
- 无法验证时标记“需要人工确认”，不能伪装成功。

### 16.4 本版本验收

- 一个包含读取、处理、生成和保存的多步骤本地任务可完整执行。
- 任一步失败时不会越过失败步骤继续产生外部影响。
- 重规划不会绕过原权限决定。
- 完成状态必须有验证证据。

---

## 17. v0.6.0：搜索与浏览器工具

### 17.1 搜索工具

- 查询、时间范围、域名范围和结果数量显式化。
- 结果保留标题、URL、发布时间、抓取时间和摘要。
- 新闻、价格、法律、软件版本等易变化信息必须标注日期。

### 17.2 浏览器工具

1. 先做打开、读取、查找和截图。
2. 再做点击、输入和下载。
3. 登录、提交表单、发帖、购买等动作提升风险等级。
4. 页面内容视为不可信数据，防止 Prompt Injection。
5. 下载文件进入隔离区并经过类型和大小检查。

### 17.3 本版本验收

- 只读浏览不会自动提交表单。
- 跨域导航、下载和登录状态有明确提示。
- 网页中的恶意指令不能修改系统规则或权限策略。
- 回答保留可点击来源和访问时间。

---

## 18. v0.6.1：办公与数据工具

按风险从低到高推进：

1. CSV/JSON 分析和转换。
2. XLSX 读取、生成和校验。
3. DOCX 创建、编辑和渲染检查。
4. PDF 提取、生成和页面验证。
5. 图片和图表生成。
6. 打开本地办公应用预览。
7. 最后才考虑控制已打开的办公应用。

每类产物都要有：模板、Schema、文件校验、视觉 QA、版本记录和来源说明。

验收场景：用户提供文本账单，遐蝶读取数据、生成 Excel 报表、验证公式和图表、展示预览，只有用户确认后才保存到目标目录。

---

## 19. v0.7.0：外部连接与消息

### 19.1 Connector 统一接口

- 连接状态和账号标识。
- 能力清单和权限范围。
- 读取、草稿、发送、更新和删除分开声明。
- 速率限制、重试、幂等键和回执。

### 19.2 推进顺序

1. 只读日历、任务或消息。
2. 创建本地草稿。
3. 用户确认后创建外部草稿。
4. 用户逐次确认后发送或更新。
5. 删除、付款、下单长期保持默认禁用。

### 19.3 外发确认卡

必须展示：目标平台、账号、收件人、正文或变更摘要、附件、风险、预计影响和是否可撤回。

### 19.4 本版本验收

- 模型不能仅凭一句“已经确认”绕过真实确认状态。
- 重试不会重复发送。
- 发送结果有平台回执和审计记录。
- Connector 断开不会损坏本地任务状态。

---

## 20. v0.8.0：受控桌面自动化

这是高风险阶段，必须在前述权限、工具、任务和恢复体系稳定后开始。

### 20.1 组件拆分

- DesktopObserver：只负责截图、窗口和控件信息。
- DesktopExecutor：只负责批准后的输入动作。
- CoordinateMapper：处理 DPI、窗口位置和缩放。
- PolicyGuard：检查应用白名单、动作和资源范围。
- EmergencyStop：独立于模型和任务循环。

### 20.2 推进顺序

1. 列出窗口和前台应用。
2. 截图和只读 UI 结构。
3. 白名单应用中的单次点击。
4. 受控文本输入。
5. 小型确定性流程。
6. 多步骤桌面流程。

### 20.3 红线

- 密码框、支付、系统安全设置、管理员窗口默认禁止。
- 坐标和窗口身份不确定时不得点击。
- 用户移动窗口或切换应用后必须重新观察。
- 始终提供全局快捷键急停。

### 20.4 本版本验收

- 125%、150%、200% DPI 下坐标映射可验证。
- 应用不在白名单或窗口身份变化时自动停止。
- 急停不依赖模型响应。
- 失败不会无限点击或无限输入。

---

## 21. v0.9.0：多 Agent Worker 化

### 21.1 何时才允许拆 Worker

同时满足以下条件后才开始：

- ToolRegistry 已稳定。
- TaskRun/TaskNode 状态机已稳定。
- 权限、审批和审计已稳定。
- 单主控能可靠完成目标场景。
- 已有明确性能、隔离或专业上下文需求，证明拆 Worker 有价值。

### 21.2 推荐拓扑

```text
CompanionAgent
  ↓
Orchestrator
  ├─ KnowledgeAgent
  ├─ FileDataAgent
  ├─ BrowserAgent
  ├─ OfficeAgent
  ├─ MemoryAgent
  └─ VerifierAgent
       ↓
ToolRegistry + PolicyGuard + Scheduler + Audit
```

### 21.3 第一批 Worker

1. KnowledgeAgent：检索、引用和知识范围控制。
2. FileDataAgent：文档读取、数据转换和产物生成。
3. VerifierAgent：独立检查结果和完成条件。

MemoryAgent、BrowserAgent、MessageAgent 和 DesktopExecutorAgent 后续按需求增加。

### 21.4 协作协议

- Worker 不直接与用户争夺最终回答权。
- Worker 不拥有永久权限，只获得任务节点级能力令牌。
- 通过结构化事件和产物通信，不依赖隐式聊天上下文。
- Scheduler 控制并发、超时、资源租约和取消。
- Orchestrator 不能自行批准高风险动作。

### 21.5 本版本验收

- Worker 失败不会拖垮整个桌面应用。
- 同一文件、浏览器会话或外部资源不会被冲突操作。
- 用户看到的是统一任务过程，而不是多个 Agent 混乱对话。
- 多 Agent 相比单主控在目标场景中有可测量收益。

---

## 22. v0.9.1：恢复、评估和长期运行

### 22.1 恢复

- 进程崩溃后恢复未完成 TaskRun。
- 区分可重试、需重规划、需人工介入和永久失败。
- 使用幂等键避免重复外发和重复产物。
- 保存有限检查点，不保存敏感临时数据。

### 22.2 评估

建立固定场景集：

- 普通聊天和长对话。
- 记忆写入、冲突、删除和召回。
- 文件读取、写入冲突和路径攻击。
- 知识检索和引用准确率。
- 多步骤任务成功率。
- 权限拒绝和 Prompt Injection。
- 浏览器断网、外部限流和工具超时。
- 应用重启、数据库迁移和任务恢复。

### 22.3 指标

- 任务完成率。
- 无人工干预完成率。
- 错误恢复率。
- 高风险误执行次数，目标必须为 0。
- 引用准确率和无法验证率。
- P50/P95 延迟、模型 token 和成本。
- 崩溃率、启动成功率和数据库迁移成功率。

---

## 23. v1.0.0：稳定发布版

### 23.1 功能门槛

- 桌宠和单主窗口体验稳定。
- 聊天、Provider、上下文、记忆、任务、工具、文件、知识和产物形成闭环。
- 至少一套多步骤 Agent 工作流通过完整验收。
- 高风险动作具备确认、取消、急停和审计。
- 用户可导出、备份、恢复和删除自己的数据。

### 23.2 安全门槛

- API Key 使用系统安全存储。
- 本地 API 有访问令牌和严格来源策略。
- 完成 Prompt Injection、路径逃逸、敏感日志和权限绕过测试。
- 未授权 Live2D 资源已经替换为原创或允许再分发的正式资产。
- 安装器、更新包和依赖清单可追溯。

### 23.3 发布门槛

- Windows 10/11 实机安装、升级、卸载和数据保留验证。
- 安装后无需 Python 和 Node。
- 应用版本、数据库 Schema 和迁移版本统一。
- 具备崩溃日志、诊断包和隐私说明。
- 代码签名和自动更新策略明确。
- README、用户手册、CHANGELOG、第三方许可和模型许可齐全。

### 23.4 v1.0 验收场景

用户导入一组项目文档，要求遐蝶整理要点并生成一份带来源的报告和任务清单。遐蝶应当：

1. 告知文件范围和数据流向。
2. 建立知识索引并显示状态。
3. 形成可见执行计划。
4. 检索并引用真实文档片段。
5. 生成报告产物和任务清单。
6. 验证文件完整性与引用。
7. 保存前请求确认。
8. 展示结果、产物、来源和审计记录。
9. 应用重启后仍可查看任务和结果。
10. 用户删除项目后，相关索引和派生数据可彻底清除。

---

## 24. v1.x：稳定内核上的扩展

### 24.1 语音与多模态

- TTS 先于 ASR。
- 语音开关、音量、隐私和数据流向明确。
- 图片理解、截图问答和视觉任务接入统一工具协议。
- 语音不能绕过文字确认卡和权限策略。

### 24.2 插件体系

- 插件有 Manifest、版本、签名、能力声明和权限清单。
- 插件运行在隔离边界，不能直接访问全部数据库和文件系统。
- 安装、升级、禁用和卸载可回滚。
- 插件市场只有在审核、签名和权限 UI 完成后考虑。

### 24.3 有边界的自我修复

允许：收集错误、定位问题、生成修复建议、生成补丁、运行隔离测试、展示 diff 和回滚方案。

禁止：静默修改稳定版本、自动提交、自动推送、自动部署或绕过人工审批。

### 24.4 专项能力

Blender、DCC、游戏、复杂办公自动化等能力以独立插件或 Worker 提供，不进入核心内核，也不得降低核心安全策略。

---

## 25. v2.0.0：成熟个人 Agent 平台

v2.0 不是“无限权限的全能 AI”，而是可组合、可解释、可恢复、由用户持续掌控的个人 Agent 平台。

最终架构应具备：

- 稳定 CompanionAgent 作为唯一用户入口。
- Orchestrator 根据任务选择专业 Worker。
- Scheduler 管理并发、资源、成本和取消。
- PolicyGuard 在所有执行路径上统一生效。
- ToolRegistry 和 ConnectorRegistry 提供可审计能力。
- Memory 与 Knowledge 分离，来源、生命周期和权限明确。
- ArtifactStore 管理所有输出和版本。
- Verifier 对关键结果进行独立检查。
- Eval 系统持续衡量质量、可靠性和安全性。
- 插件和 Worker 可以扩展，但不能绕过核心治理。

v2.0 的最终成功标准：普通用户能够把一个真实、复杂、跨资料和工具的目标交给遐蝶；用户始终能看见她准备做什么、正在做什么、为什么需要权限、产生了什么结果，并能随时暂停、拒绝、回滚和删除。

---

## 26. 版本推进和暂停规则

### 26.1 可以进入下一阶段的条件

- 当前版本所有必须验收项通过。
- 没有未处理的高危安全问题。
- 数据迁移和回滚已经验证。
- 文档与代码一致。
- 工作区干净，提交已推送并打阶段标签。

### 26.2 必须暂停扩展的情况

- 出现数据丢失、权限绕过或密钥泄露。
- 测试持续不稳定，无法判断失败原因。
- 为实现新功能需要一次性修改过多模块。
- 旧数据迁移路径不明确。
- 外部平台授权或素材许可不清楚。
- 当前基础能力尚未被真实使用场景验证。

### 26.3 允许调整路线的方式

1. 先记录现状和调整原因。
2. 创建 ADR，列出方案、替代项、影响和回滚条件。
3. 只修改受影响阶段，不静默重写全部路线。
4. 保留已完成版本和历史决策。

---

## 27. 从现在开始的前十个小任务

这些任务是当前唯一推荐的近期入口，按顺序逐个完成：

1. 创建 `docs/CODEX_PROJECT_CONTEXT.md`，固化不可变决策。
2. 创建 `docs/BASELINE_STATUS.md`，记录当前测试、构建和运行基线。
3. 创建 `docs/PR_CHECKLIST.md` 和 ADR 模板。
4. 为本地 FastAPI 增加桌面端访问令牌设计和测试。
5. 收紧 CORS 与健康接口暴露范围。
6. 建立 SecretStore 接口和 safeStorage 迁移方案。
7. 修复重新生成失败导致旧回复丢失的问题。
8. 增加 Provider/Model 严格校验和统一错误结构。
9. 建立聊天上下文预算和长会话摘要设计。
10. 完成 v0.1.1/v0.1.2 的全量回归、文档更新和版本标签。

完成这十项之前，不开始文件写入、浏览器操作、外部消息、桌面自动化和多 Agent。

---

## 28. 路线维护记录

| 日期 | 变更 | 原因 |
| --- | --- | --- |
| 2026-07-14 | 创建长期路线初版 | 将旧设计与当前 v0.1.0 实现重新对齐，确定从安全基线到最终 Agent 的渐进路线。 |
| 2026-07-14 | 加入伴侣状态与记忆星座阶段 | 融合 jiwen 与 MemoryConstellations，并保持单后端、可追溯和受控主动性。 |
| 2026-07-20 | 完成 CTX.0～CTX.7 上下文专项内部总验收 | 冻结硬预算、滚动摘要、跨会话显式回忆和用户控制；普通自动召回继续 shadow，产品主线回到陪伴式受控 Agent 地基。 |
| 2026-07-20 | CTX.7 独立 strict review 通过并正式关闭专项 | 审查确认 0 个未解决 P0/P1；shadow 与 Provider token 校准保留为独立后续工作，摘要不得直接写长期记忆。下一专项转向情绪、关系积温与主动陪伴。 |
| 2026-07-22 | EAP v0.2 完成度审计并重开收口 | Schema 48～55、领域模块和测试已落地，但只有 Presence hook 接入主链；真实编排、投递、反馈、设置硬边界和关系意义应用未闭环。改以 EAP.R0～R6 为唯一入口，LIFE 延后到 EAP 正式冻结之后。 |
| 2026-07-22 | EAP.R4 本机投递安全闭环 | Schema 59 建立唯一 Delivery/attempt/event 账本；Level 0～4 接入后端最终门禁与 Electron，本机真实投递显式实验开启且默认关闭，Level 5 硬禁用。下一阶段为 R5 反馈、历史与用户控制。 |
| 2026-07-22 | EAP.R5 反馈控制闭环与 R6 生产总验收施工 | Schema 60 完成 grounded feedback、历史与清除；生产模拟覆盖 15 分钟至 30 天及休眠、回拨、断网、Provider、崩溃、busy，新增 Windows resume 保护。协议保持 IMPLEMENTED，停线等待 R6 strict review；通过前不启动 LIFE/KIG。 |
| 2026-07-22 | EAP.R6 strict review 通过并正式关闭专项 | 0 个 P0/P1，10/10 冻结指标通过；六个 EAP 协议与 Schema 60 正式冻结。后续专项固定为 CDS → LIFE → KIG，避免重复建设 DecisionRun、语义重排和跨源治理。 |
| 2026-07-22 | CDS/LIFE/KIG v0.3 施工计划入库 | 采纳交叉专项 review：新增 ConstructionBaseline、唯一所有权矩阵、统一晋级/模型认证/预算/数据生命周期；LIFE 补单写者与真实性治理，KIG 拆为 KIG-R/KIG-P。PR #1 合并并锁定 SHA 前不启动 CDS 迁移施工。 |
| 2026-07-22 | CDS.0 离线基线施工完成，等待 review | PR #1 已合并；以 `6b8aa47134f8a9a55131c73bb1148e6912421c4f`、Schema 60 和 `937 passed, 1 warning` 为 ConstructionBaseline，冻结六条旧算法轨道并建立 300 个纯合成场景。未改变聊天行为或占用 Schema 61，review 通过前不进入 CDS.1。 |
| 2026-07-22 | CDS.0 review 通过并完成 CDS.1 共享运行时施工 | CDS.0 strict review 确认 0 P0/P1；CDS.1 以 Schema 61 扩展唯一 `decision_runs`，实现 `cognitive-decision-v1`、专属 Schema 注册、候选白名单、多来源复核、三模式、一次 JSON 修复、公共事件与无正文诊断。当前仅合成 probe Shadow，等待 CDS.1 review。 |
| 2026-07-22 | CDS.1 review 通过并完成 CDS.2 模型运行时施工 | CDS.1 strict review 确认 0 P0/P1；Schema 62 实现逻辑角色路由、位置 fail-closed、binding 认证、合成 structured probe、per-kind 超时/熔断、统一 fallback 与预算/取消控制面。当前仍仅合成 probe Shadow，等待 CDS.2 review。 |
| 2026-07-22 | CDS.2 review 通过并完成 CDS.3 Presence/Thread 校准 | CDS.2 strict review 确认 0 P0/P1；采纳 binding 版本、角色 probe timeout 与预算清理建议。新增只读 Shadow 协议和 660 轮纯合成评测，三项门为 0%/100%/0%；EAP v2 未修改，语义差异仅形成 v3 提案，等待 CDS.3 review。 |
| 2026-07-22 | CDS.3 review 通过并完成 CDS.4 RecallPlanner Shadow | CDS.3 strict review 确认 0 P0/P1，两项 P2 均采纳，Presence 固定集扩为 900 轮。新增只读 RecallPlanner 协议和 600 轮纯合成对照；任务/来源需求及必需来源召回 100%，禁止检索违规 0%。现有检索与 ContextPackage 未改，等待 CDS.4 review。 |
| 2026-07-26 | CDS.0～10 review 后审计与定点返工 | 核实 CDS.4～9 无需推倒重做；新增失败测试发现 CDS.10 Episode/Saga 语义动作矩阵未完全闭合，已同步收紧 validator 与独立 oracle v3。CDS.6 修复 final+done 重复最终回调；后端 2284 项、前端 45 项通过。CDS.10 小样本质量仍为 50%，禁止据此晋级，未进入 CDS.11。 |
| 2026-07-27 | LIFE.0～13 技术施工完成，等待总体 Review | Schema 64～71、生活事件/时钟/CatchUp/日程/目标/日期/日记/SelfTimeline、LIFE→EAP adapter 与产品页完成；180 天、30 天、100 时区日期、100 来源层级及保守压缩演练通过。后端 2416、前端 50、当前 Windows 构建、NSIS 临时安装、托盘/崩溃/重启/卸载和休眠唤醒保护场景通过；自然度与 0 P0/P1 总 Review 未完成，LIFE 尚未冻结，KIG 不开工。 |
| 2026-07-28 | 登记 LIFE v2 长期候选基线 | 将 `structured-inner-state-v1` 与 `short-memo-v1` 固定为 LIFE v2 候选，明确非 CIE 前置、CIE 不得越权实现，并保留协议、迁移与排期未分配状态，便于未来专项检索与重新评审。 |
| 2026-07-28 | CIE.1 消息积累窗口技术施工完成 | CIE.0 Review 通过后实现默认 500 ms 的有界 TurnIngressBuffer；原消息和附件分别持久化，服务端重建瞬态 envelope，625 条矩阵五项零容忍指标均为 0。Schema 保持 80，等待独立 Review 后再进入生成打断。 |
| 2026-07-29 | CIE.0～CIE.6 正式冻结 | 最终独立 Review 确认 0 P0/P1；后端 2597、前端 71、Electron contract/Windows 当前源码烟测、发布资源与 10 项零容忍指标全部通过。Schema 81 与 CIE v1 协议冻结；两个 P2 登记为兼容维护候选。下一步先做 LIFE v2 立项审计，不直接施工。 |
| 2026-07-29 | LIFE v2 立项审计与讨论稿 | 审计现有 Persona/Affect/Relationship/CTX/Lore 装配链，将人格提示词版本化、模型评测和回退编译纳入 LIFE v2，同时把 ShortMemo 与 StructuredInnerState 分开过门。当前不改运行时、不占用 Schema 82，等待用户讨论。 |
| 2026-07-29 | LIFE v2 Persona 与 WorldBook 内容讨论推进 | Persona Core、双模式、有限风格和负面矩阵已确认冻结；WorldBook `self/daily_life/events` 通过内容范围 Review，逐火条目按黄金裔 WIKI 3.7 补齐关键主干。来源与优先级仍待最终冻结，下一批进入 `characters`，未授权运行时施工或 Schema 82。 |
| 2026-07-29 | LIFE v2 Persona / WorldBook r1 内容治理完成 | 30 个 WorldBook 条目和受限 Glossary 已完成内容、来源分级、唯一所有者与预算 Review；当前 `verified_a=0`、`candidate_b=27`、`local_candidate=3`，最坏三节实测 2185/3600 字符。旧 Lore 暂不覆盖；关联超过两条按 `priority DESC, entry_id ASC` 确定性取前二。下一步讨论 ShortMemo 与是否取消持久化 StructuredInnerState，仍未授权代码或 Schema 82。 |
| 2026-07-29 | LIFE v2 ShortMemo / InnerState 范围确认 | ShortMemo 在用户总开关开启后允许静默创建，默认 72 小时、范围 1 小时～14 天、最多 10 条，只在相关时召回且不写其他状态域；敏感事项最小化，秘密值硬拒绝。持久化 StructuredInnerState 正式取消，改为当轮只读 `InnerStateProjection`，不占用迁移。下一步为计划独立 Review，尚未授权施工。 |
| 2026-07-29 | LIFE v2 v0.3 construction candidate 补齐 | 冻结 Schema 82 ShortMemo、LIFE API、独立发布门、请求内 `inner-state-projection-v1`、A 级 WorldBook 生产来源门、分段施工卡、测试硬门与非破坏性回滚；首次迁移保持 Shadow。下一步进行计划独立 Review，通过后从 LIFE2.0 基线冻结开始逐段施工。 |
| 2026-07-29 | LIFE v2 v0.3 计划 Review 通过并冻结 | 独立 plan-review 原有 2 个 P1、3 个 P2 全部采纳：补齐 ShortMemo Shadow→Active 请求边界、会话级联删除权威语义、事件 metadata 白名单、容量拒绝聚合路径和 WorldBook 独立缓存。未解决 P0/P1/P2 为 0；下一步提交计划并完成 LIFE2.0 ConstructionBaseline，不提前占用 Schema 82。 |
| 2026-07-30 | LIFE2.0 ConstructionBaseline 完成 | 冻结计划提交 `303ce2c02a7c19584a8a28199a2ddf58e61b3a8f` 作为干净基线，Schema 81；项目 `.venv` 后端 2597/2597、前端 71/71 与生产构建通过。系统 Python 缺失 tzdata 的误调用不计入产品基线。下一步进入 LIFE2.1，不提前修改运行时或 Schema 82。 |
| 2026-07-30 | LIFE2.1 Persona 真实模型基线完成 | 建立 120 条纯合成固定集与确定性硬门评分器；`deepseek-v4-flash` 旧 Persona 三轮共 360 次调用均成功，硬门通过 97/120、97/120、99/120。主要缺口为动作旁白、工具事实和医疗边界；旧版不晋级。下一步实现可回退 Persona 编译器与 WorldBook r1 Shadow。 |
